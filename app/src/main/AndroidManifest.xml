<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Manifest Matchmaking"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".RemoveRangerList" />
        <activity android:name=".ViewRanger" />
        <activity android:name=".RangerList" />
        <activity
            android:name=".ChangeAircraft"
            android:label="Change Aircraft Details" />
        <activity android:name=".ManifestList" />
        <activity
            android:name=".ViewManifest"
            android:label="View Manifest" />
        <activity
            android:name=".CreateManifest"
            android:label="Create Manifest" />
        <activity
            android:name=".CreateRanger"
            android:label="Create Ranger" />
        <activity
            android:name=".MainActivity"
            android:label="Manifest Matchmaking" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>